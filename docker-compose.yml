version: '3.8'

services:
  clinic_appointments_app:
    build: .
    ports:
      - "4200:4200"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: ["ng", "serve", "--host", "0.0.0.0", "--poll", "2000"]
